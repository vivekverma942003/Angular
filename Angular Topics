ngFor
what is utility first meaning in the tailwind css concept 


in tailwind the syntax for providing the background and color to the text are :-
text-{color}-{shade}
<p class="text-red-500">Red Text</p>
<p class="text-green-600">Green Text</p>


Background Color Utility Classes
Similar pattern to text color, but using bg- prefix.
✅ Syntax:
html
Copy
Edit
bg-{color}-{shade}


🧠 Combine Text & Background Colors
html
Copy
Edit
<div class="bg-green-500 text-white p-4 rounded">
  Success Message
</div>

🧠 1. Margin (m)
🔹 Syntax:
css
Copy
Edit
m-{value}       → All sides  
mt-{value}      → Top  
mb-{value}      → Bottom  
ml-{value}      → Left  
mr-{value}      → Right  
mx-{value}      → Left + Right  
my-{value}      → Top + Bottom

                              m-0     → 0px  
                              m-1     → 0.25rem (4px)  
                              m-2     → 0.5rem (8px)  
                              m-4     → 1rem (16px)  
                              m-8     → 2rem (32px)






we can also provide the negative margin to the elements by definfing the - sign before the m or p text in the tailwind css 















